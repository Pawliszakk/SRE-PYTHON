- fix get logs from file function to get one log after other to not load all logs into memory
- add custom ip selection to script
- add fallback or default metric if no args show are provided

1. --show-gr